environment:
  matrix:
    #- nodejs_version: "7"
    - nodejs_version: "8"

platform:
  #- x86
  - x64

install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm install
  - ps: Start-FileDownload 'https://ffmpeg.zeranoe.com/builds/win64/static/ffmpeg-3.4.1-win64-static.zip'
  - 7z x ffmpeg-3.4.1-win64-static.zip -oC:\ffmpeg
  - cd C:\ffmpeg
  #- cd ffmpeg
  #- cd ffmpeg-3.4.1-win64-static
  - dir /s
  #- cd bin
  #- dir /s
  #- ffmpeg.exe -version

test_script:
  - node --version
  - npm --version
  - npm test

build: off